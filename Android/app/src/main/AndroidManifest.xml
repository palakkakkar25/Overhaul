<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mdo3.overhaul">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- To allow the app to use a map on the users' main screens. -->
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyDTdFj-O2k2pYBCfXCaHB0c_kyqq_gKZtM" />

        <activity
            android:name=".HomeScreen"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DriverMainScreen" />
        <activity android:name=".DriverSettings" />
        <activity android:name=".ClientMainScreen" />
        <activity android:name=".JobRequest" />
        <activity android:name=".UserRegistration" />
        <activity android:name=".DriverRegistration" />
        <activity android:name=".DriverRequestScreen" />
        <activity android:name=".WaitScreen" />
        <activity android:name=".RateScreen" />
        <activity
            android:name=".credit_card_info"
            android:parentActivityName=".HomeScreen" />
        <activity
            android:name=".driver_acc_info"
            android:parentActivityName=".HomeScreen" />
        <activity
            android:name=".DriverDetails"
            android:label="@string/title_activity_driver_details"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".UpdateCustomerInformation" />
        <activity android:name=".DriverVehicleUpdateActivity"></activity>
    </application>

</manifest>